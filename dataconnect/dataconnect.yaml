specVersion: "v1"
serviceId: "testpro"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "testpro-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
